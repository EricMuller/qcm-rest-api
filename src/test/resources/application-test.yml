# Security enable/disable
security:
  ignored: /**
  basic:
    enabled: false

jpa:
    generate-ddl: false
    show-sql: true
    properties:
              format_sql: true

keycloak:
  enabled: false


spring:
  security:
    enabled: false

management:
  security:
    enabled: false
